c programming notes

c programming notes a single 1 or 0 stored in the computer is called a ‘bit’ most data and information stored requires a minimum of 8 bits to represent either a number or a single character such as the letter ‘a’. a combination of 8 bits is called a ‘byte’ 用8 bits的歌总组合 可以表现256种不同的’thing’ (2^8=256) ASCII （mapping characters to a binary value) number: integers and real numbers (floats and double) A program typically stores a lot of different types of information in memory. These locations in memory are called memory addresses; You need to decide in advance the type and size of the information that might be stored in memory while the program is executing. variable: can be up to 63 characters long for the C99 standard. can use uppercase and lowercase letters, digits, and the underscore; the first character must be letter or underscore. declare a variable的时候必须定义该variable的类型 例子： int year;
printf(); is part of C’s standard library of functions. located in a module called “stdio.h” (standard Input Ouput)
Type int has a size of 4 bytes.

#define CONSTNAME value

getchar() and putchar() only work with individual ASCII characters. 
 
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
for loop （起始条件（initialize）；结束条件(test)；每次都转一遍(update)）括号里的很灵活
